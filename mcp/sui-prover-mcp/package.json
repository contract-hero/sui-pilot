{
  "name": "@sui-move/sui-prover-mcp",
  "version": "0.1.0",
  "description": "MCP server wrapping the asymptotic-code sui-prover binary",
  "main": "dist/index.js",
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run test/integration/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.24.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
