---
title: '@mysten/seal'
description: Migrate @mysten/seal to 2.0 with the new registration function pattern.
keywords:
  - migration
  - '@mysten/seal'
  - Seal
  - '2.0'
  - client extension
  - encryption
  - breaking changes
questions:
  - What changed in the @mysten/seal migration?
  - How do I migrate to @mysten/seal?
answer: Migrate @mysten/seal to 2.0 with the new registration function pattern.
goal:
  description: >-
    Reader can migrate @mysten/seal to 2.0 with the new registration function pattern
  requires:
    - has_frontmatter:
        - title
        - description
        - keywords
      label: Has required frontmatter fields
    - min_words: 10
      label: Needs content depth
    - has_questions: true
      label: Needs questions for AI search visibility
    - has_answer: true
      label: Needs answer summary for AI citation
---

The deprecated `SealClient.asClientExtension()` static method has been removed. Use the `seal()`
registration function instead:

```diff
- import { SealClient } from '@mysten/seal';
+ import { seal } from '@mysten/seal';

- const client = suiClient.$extend(SealClient.asClientExtension());
+ const client = suiClient.$extend(seal());
```
