---
title: useWallets
description: React hook to list all available Sui wallets detected by dApp Kit.
keywords:
  - useWallets
  - React hook
  - available wallets
  - wallet detection
  - dApp Kit
  - Sui
questions:
  - What does the useWallets hook do?
  - How do I use useWallets?
  - How does Return value work?
answer: React hook to list all available Sui wallets detected by dApp Kit.
goal:
  description: >-
    Reader can use the React hook to list all available Sui wallets detected by dApp Kit
  requires:
    - has_frontmatter:
        - title
        - description
        - keywords
      label: Has required frontmatter fields
    - min_words: 30
      label: Needs content depth
    - has_questions: true
      label: Needs questions for AI search visibility
    - has_answer: true
      label: Needs answer summary for AI citation
---

The `useWallets` hook provides access to all available wallets.

<Callout type="warn">
	All dApp Kit hooks must be used within components that are descendants of
	[`DAppKitProvider`](../dapp-kit-provider). Using them outside will result in an error.
</Callout>

## Usage

```tsx
import { useWallets } from '@mysten/dapp-kit-react';

export function MyComponent() {
	const wallets = useWallets();

	return (
		<div>
			{wallets.map((wallet) => (
				<div key={wallet.name}>
					<img src={wallet.icon} alt={wallet.name} />
					<span>{wallet.name}</span>
				</div>
			))}
		</div>
	);
}
```

## Return value

```tsx
UiWallet[]
```
