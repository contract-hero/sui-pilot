---
title: useCurrentAccount
description: React hook to get the currently selected wallet account in a Sui dApp.
keywords:
  - useCurrentAccount
  - React hook
  - wallet account
  - current account
  - dApp Kit
  - Sui
questions:
  - What does the useCurrentAccount hook do?
  - How do I use useCurrentAccount?
  - How does Return value work?
answer: React hook to get the currently selected wallet account in a Sui dApp.
goal:
  description: >-
    Reader can use the React hook to get the currently selected wallet account in a Sui dApp
  requires:
    - has_frontmatter:
        - title
        - description
        - keywords
      label: Has required frontmatter fields
    - min_words: 30
      label: Needs content depth
    - has_questions: true
      label: Needs questions for AI search visibility
    - has_answer: true
      label: Needs answer summary for AI citation
---

The `useCurrentAccount` hook provides access to the currently selected account.

<Callout type="warn">
	All dApp Kit hooks must be used within components that are descendants of
	[`DAppKitProvider`](../dapp-kit-provider). Using them outside will result in an error.
</Callout>

## Usage

```tsx
import { useCurrentAccount } from '@mysten/dapp-kit-react';

export function MyComponent() {
	const account = useCurrentAccount();

	if (!account) {
		return <div>No account selected</div>;
	}

	return (
		<div>
			<p>Address: {account.address}</p>
			<p>Label: {account.label}</p>
		</div>
	);
}
```

## Return value

```tsx
UiWalletAccount | null;
```
